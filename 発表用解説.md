# 発表用解説：アルゴリズムって何だろう？

## 30秒で言うなら

アルゴリズムとは、**目的を達成するための、はっきり決められた手順**です。

たとえば「隼人駅から鹿児島高専まで、いちばん短い道を見つけたい」とします。
人なら地図を見て直感で選べますが、コンピュータには「どの候補から、どんな順番で調べるか」を細かく教える必要があります。その手順がアルゴリズムです。

## 地図をコンピュータが読める形にする

道路地図を、次の2つに置き換えます。

- 交差点・分岐・行き止まり → **ノード（点）**
- 交差点どうしを結ぶ道路 → **エッジ（線）**

するとナビの問題は、「スタートの点からゴールの点まで、どの線を通ればよいか」というグラフの問題になります。

## GIF 1：BFS（幅優先探索）

BFSは、スタートから近い「段数」の場所を順番に調べます。

1. 隼人駅を候補に入れる
2. 候補から最初の1地点を取り出す
3. そこから1本で行ける道路を全部調べる
4. 新しく見つけた地点を候補の最後に入れる
5. ゴールを見つけるまで繰り返す

水面の波紋のように探索が広がるのがポイントです。
ただし「道路1区間＝1」と数えるので、**通る区間数は最小でも、距離は最短とは限りません**。

## GIF 2：ダイクストラ法

ダイクストラ法は、道路の実際の長さを使います。

1. 隼人駅までの距離を0 mにする
2. 候補の中から「ここまでの合計距離」が最短の地点を選ぶ
3. その地点を通ると、となりの地点まで何mになるか計算する
4. 前より短く行けるなら、距離と「どこから来たか」を更新する
5. ゴールの最短距離が確定するまで繰り返す

選ぶ基準は `g = スタートからここまでの距離` です。
距離が負でない道路なら、ゴールが確定した時点でそのルートが距離最短になります。

## GIF 3：A*（エースター）探索

A*はダイクストラ法に「ゴールはだいたいどちらか」という予想を足します。

- `g`：隼人駅からここまでに実際に進んだ距離
- `h`：ここから鹿児島高専までの直線距離（残りの予想）
- 選ぶ基準：`f = g + h`

ゴールから遠ざかる候補を後回しにできるので、同じ最短ルートを、より少ない探索で見つけられます。
今回のデータでは、ダイクストラ法が985地点を確定したのに対し、A*は179地点でした。
探索開始から経路復元までの今回の実測時間は、BFS 1.39 ms、ダイクストラ法 5.56 ms、A* 1.69 msでした。とても短い処理なので値は実行環境やその時の負荷で変わります。

## 3つを比べて伝えたいこと

| 方法 | 何を最小にする？ | 探索済み地点 | 今回の実測 | 注意点 |
|---|---|---:|---:|---|
| BFS | 通る道路区間の数 | 774 | 1.39 ms | 道の長さが違うと距離最短ではない |
| ダイクストラ法 | 道路の合計距離 | 985 | 5.56 ms | 広い方向まで探索しやすい |
| A* | `ここまで + 残り予想` | 179 | 1.69 ms | 正しい予想関数が必要 |

大切なのは、アルゴリズムによって**答えだけでなく、答えにたどり着くまでの調べ方や計算量が変わる**ことです。

## 締めの一言

ナビは最初から正解の道を知っているわけではありません。候補を決められた順番で調べ、距離を更新し、最後に「どこから来たか」を逆にたどってルートを作ります。目に見えないこの手順こそが、ナビを動かすアルゴリズムです。
